= form_with(model: store, local: true) do |form|
  - if store.errors.any?
    #error_explanation
      %h2
        = pluralize(store.errors.count, "error")
        prohibited this store from being saved:
      %div
        = form.label '名前', class: "col-sm-3 control-label"
        = form.text_field :name, placeholder: '', require: true
      %div
        = form.label '内容', class: "col-sm-3 control-label"
        = form.text_field :detail, placeholder: '', require: true
  .actions
    = form.submit
