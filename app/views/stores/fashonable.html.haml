#contents
  .inner
    %section
      %h2
        おしゃれ飯
        %span List of Reccomendations
      %ul.list2
        - @stores.each do |store|
          %li
            = link_to store_path(store.id) do
              = store.name
              = store.health
              = store.fashonable
              = store.delicious
